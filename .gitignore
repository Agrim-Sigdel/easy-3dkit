node_modules
dist
dist-site
dist-ssr
*.local
.DS_Store
.vite
*.log
*.tsbuildinfo

# Packed npm tarballs (produced by `npm pack`)
*.tgz

# Generated copy of the starter (synced from templates/starter by `pnpm sync:starter`
# before publishing create-easy-3dkit). templates/starter is the source of truth.
packages/create-easy-3dkit/template/

# Test artifacts (vitest)
coverage/
.vitest/

# Stray probe / scratch dirs that have accidentally landed at the repo root
/tmp/
/var/
probe/
o3s-probe/

# Editor / OS
.idea/
.vscode/
*.swp
Thumbs.db

# Local planning/design docs (root only). Anchored so the app's own src/docs/
# (the published docs site) is NOT ignored.
/docs/